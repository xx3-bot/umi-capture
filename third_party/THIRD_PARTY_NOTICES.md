# Third-party notices

The iOS source depends on Socket.IO-Client-Swift 16.1.0 (MIT) and Starscream
4.0.8 (Apache-2.0), installed by users through CocoaPods. Their license texts
are included in `third_party/licenses/`.

The Python Receiver source depends on python-socketio 5.11.4 (MIT) and eventlet
0.37.0 (MIT), installed by users through pip. The repository does not
redistribute their wheels. Versioned upstream source and license links for all
four dependencies are listed in `PUBLIC_DEPENDENCIES.md`.

This source snapshot does not redistribute CocoaPods frameworks, Apple Python,
NumPy, OpenCV, FFmpeg, the private macOS GUI or workers, or any private runtime
payload.

ARKit, AVFoundation, SwiftUI, and other Apple SDK frameworks are platform
dependencies supplied under Apple's applicable developer terms; they are not
redistributed as project source.
